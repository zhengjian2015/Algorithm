package com.datastruct;

/**
 * 引出哈希的概念
 * 用空间换时间
 *
 *  键 变为 索引 的 哈希函数
 *  不一定 每个键队医唯一的索引   这就是哈希冲突
 *   哈希是 空间和 时间的平衡
 *   假设有无限的空间所有 时间辅助度是 1
 *    假设只有1的空间， 每个都会产生冲突，时间就是 n
 *   但是
 */
public class Hash {
}
